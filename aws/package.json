{
  "name": "mechpro-aws",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AWS-native backend for MechPro: Convex-compat runtime, Lambda handlers, and CDK infrastructure.",
  "scripts": {
    "test": "node --experimental-strip-types --test --test-concurrency=1 runtime/context.unit.test.ts runtime/context.test.ts runtime/db.test.ts runtime/functions.test.ts runtime/ported.test.ts runtime/storage.test.ts",
    "test:storage-integration": "node --experimental-strip-types --test runtime/storageDeletions.integration.test.ts",
    "smoke:storage-deletion-worker": "node scripts/smoke-storage-deletion-worker.mjs",
    "db:up": "docker run -d --name mechpro-pg -e POSTGRES_PASSWORD=devpw -e POSTGRES_DB=mechpro -p 55433:5432 postgres:16",
    "db:schema": "docker cp db/schema.sql mechpro-pg:/tmp/schema.sql && docker exec mechpro-pg psql -U postgres -d mechpro -v ON_ERROR_STOP=1 -f /tmp/schema.sql"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1104.0",
    "@aws-sdk/client-secrets-manager": "3.1104.0",
    "@aws-sdk/s3-request-presigner": "^3.1104.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@types/pg": "^8.11.10",
    "esbuild": "^0.28.1",
    "typescript": "~5.9.2"
  }
}
